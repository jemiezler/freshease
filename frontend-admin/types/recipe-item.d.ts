export type RecipeItem = {
	id: string;
	recipe_id: string;
	product_id: string;
	amount: number;
	unit: string;
};

export type RecipeItemPayload = {
	id: string;
	recipe_id: string;
	product_id: string;
	amount: number;
	unit: string;
};

